# 컬러스위퍼 패치노트 — 구축 가이드 (관리자용)

> 최초 1회 세팅용. 이관·재구축·감사 때 이 문서를 보면 됩니다.
> 매 차수 운영은 `2_팀원용_1페이지.md`, 본문 작성 규칙은 `4_본문_작성규칙.md`.

---

## 1. 왜 파일이 3개인가

한 곳에 다 넣으면 **시트를 공유하는 순간 GitHub 토큰도 같이 공유**됩니다. 시트 편집자는 확장 프로그램 → Apps Script를 열어 스크립트 안의 모든 값을 볼 수 있어서, 스크립트에 비밀을 숨기는 것도 소용없습니다.

| 장소 | 공유 | 들어있는 것 |
|---|---|---|
| **① GitHub 저장소** | Public (사이트 파일만) | `index.html`, `data/`, `images/` |
| **② Drive 이미지 폴더** | 팀원 편집자 | 차수별 하위 폴더 |
| **③ 배포 콘솔 시트** | 🔒 관리자만 | GitHub 토큰, OpenAI 키, 권한자 목록, 배포 로그 |
| **④ 마스터 시트** | 팀원 편집자 | 원문·번역·메뉴. **비밀정보 없음** |

## 2. 어떻게 도는가 (Queue 방식)

```
④ 마스터 시트에서 메뉴 클릭
   → Queue 탭에 요청 한 줄 기록
   → ③ 배포 콘솔이 1분마다 확인 (시간 트리거)
   → 요청자를 Access 탭과 대조해 권한 검사
   → 마스터 시트에서 원문·번역을 직접 읽어 JSON 생성
   → ② Drive 이미지를 ① GitHub에 커밋
   → GitHub Pages가 사이트 반영 (1~2분)
```

웹앱·OAuth 토큰 교환이 없습니다. 회사 Workspace 정책의 영향을 받지 않는 게 이 방식을 택한 이유입니다.

**남은 한계**: 요청자 이메일을 마스터 스크립트가 적어 보내므로, 팀원이 마스터 스크립트를 고의로 수정하면 남의 이름으로 배포할 수 있습니다. 시도 기록은 `Log` 탭에 남습니다. 사내 동료 대상이라 실질 위험은 낮다고 판단했습니다.

---

## 3. ① GitHub 저장소

1. 저장소 생성 (Public — Pages 무료 조건)
2. `site/` 폴더 **안의 내용물**을 저장소 루트에 업로드
   → 최상단에 `index.html`이 보여야 정상. `site` 폴더가 통째로 올라가면 잘못된 것
3. `Settings` → `Pages` → Source `Deploy from a branch` → `main` / `/ (root)` → Save

### 도메인 (선택)

| 방식 | DNS |
|---|---|
| 서브도메인 (권장) | `CNAME  patchnote → <계정>.github.io` |
| 루트 도메인 | `A` 4개 — `185.199.108.153` / `.109.153` / `.110.153` / `.111.153` |

`Settings` → `Pages` → Custom domain 입력 → **Enforce HTTPS**.
리다이렉트가 아니라 CNAME 연결을 쓰세요. 리다이렉트는 인게임 웹뷰 도메인 화이트리스트에 걸립니다.

### 토큰

`프로필` → `Settings` → `Developer settings` → `Personal access tokens` → **Fine-grained**

- Repository access: **이 저장소 하나만**
- Permissions → **Contents: Read and write** (이것만. Metadata는 자동 추가)
- 만료 1년 → **캘린더에 갱신 알림 필수**

---

## 4. ② Drive 이미지 폴더

내 드라이브에 폴더 생성 → 팀원에게 **편집자** 공유.
그 안에 **차수 이름과 정확히 같은** 하위 폴더를 만듭니다.

```
컬러스위퍼_패치노트_이미지/
└── 1.24.0/
    ├── hero.png          ← 전 언어 공용
    ├── hero@ja.png       ← 일본어에서만 교체
    └── book.png
```

파일명 규칙 `이미지키[@언어코드].확장자` / png·jpg·gif·webp / **개당 2MB 이하**
폴더 URL의 `folders/` 뒤 문자열이 **폴더 ID**입니다.

---

## 5. ③ 배포 콘솔 시트 (🔒 절대 공유 금지)

새 스프레드시트 → `[비공개] 패치노트 배포 콘솔`

### Config 탭

| key | value |
|---|---|
| `github_owner` | 깃허브 계정명 |
| `github_repo` | 저장소명 |
| `github_branch` | `main` |
| `github_token` | `github_pat_...` |
| `drive_root_id` | Drive **부모** 폴더 ID |
| `master_sheet_id` | 마스터 시트 URL의 `/d/` 뒤 ID |
| `openai_api_key` | `sk-...` (MT 초벌용, 선택) |
| `openai_model` | 선택. 기본 `gpt-4.1-mini` |

### Access 탭 — 권한 관리의 전부

| email | role | name | memo |
|---|---|---|---|
| rosiekim@com2us.com | admin | 옌 | |

| role | 가능한 것 |
|---|---|
| `admin` / `publisher` | 작성 · MT · 검증 · **배포 · 내리기** |
| `editor` | 작성 · MT · 검증 |
| 목록에 없음 | 차단 (시도 기록은 Log에 남음) |

**권한 주기 = 한 줄 추가, 회수 = 한 줄 삭제.** 1분 안에 반영됩니다.

### Log 탭

1행 헤더만: `시각` / `이메일` / `액션` / `버전` / `결과`

### 스크립트 + 트리거

1. `확장 프로그램` → `Apps Script` → `console/Console.gs` 붙여넣기 → 저장
2. 왼쪽 **⏰ 트리거** → `트리거 추가`
   - 함수 **`processQueue`** / 이벤트 소스 **시간 기반** / **분 타이머** / **1분마다**
3. 권한 승인 시 **`Select all` 체크** (Drive 읽기가 반드시 포함되어야 함)

확인: 시트 새로고침 → 🔐 배포 콘솔 → `GitHub 연결 테스트` / `Drive 폴더 점검` / `마스터 시트 연결 테스트`

---

## 6. ④ 마스터 시트 (팀 공유)

새 스프레드시트 → `컬러스위퍼 패치노트`

### Config 탭

| key | value |
|---|---|
| `drive_folder_url` | Drive 부모 폴더 링크 |

### Versions 탭 (1행 헤더)

```
version	date	tags	update_type	hero_image_key	title_ko	summary_ko
```

### Content 탭 (1행 헤더)

```
version	order	section_key	section_title_ko	tag	type	key	ko	image_key	max_len	note
```

### Glossary 탭 (선택, MT 품질에 중요)

```
ko	en	ja	zh-Hans	...
```

### 스크립트

`확장 프로그램` → `Apps Script` → `master/Code.gs` + `master/appsscript.json` 붙여넣기 → 저장

> `appsscript.json`을 빠뜨리면 Drive 권한이 없어 이미지 확인이 실패합니다.
> 매니페스트는 ⚙️ 프로젝트 설정에서 "편집기에 표시"를 켜야 보입니다.

### 마무리

- `Config` 탭 → 우클릭 → 시트 보호 → 나만 편집
- 팀원에게 **편집자**로 공유
- `Queue`·`_Roles` 탭은 자동 생성 — 직접 만들지 말 것

---

## 7. 연습 배포로 전체 점검

1. Drive `1.24.0` 폴더에 `hero.png` (2MB 이하)
2. `Versions`에 한 줄 / `Content`에 한 줄
3. 📋 → `4. 이미지 확인` → `hero.png → key=hero / common`
4. 📋 → `1. 번역 시트 생성` → `T_1.24.0` 생성
5. 📋 → `2. MT 초벌 채우기` → `en` (또는 en 컬럼 직접 입력)
6. 📋 → `3. 번역 상태 검증` → `✅ en 100%`
7. 📋 → `5. 사이트에 배포` → 최대 2분 → `✅ 배포 완료`
8. 사이트 새로고침 + 콘솔 `Log` 탭 확인

**급할 때**: 🔐 배포 콘솔 → `지금 대기열 처리` 로 1분 대기 없이 즉시 처리.

---

## 8. 문제가 생기면

| 증상 | 볼 곳 |
|---|---|
| 요청이 계속 `대기` | 콘솔 ⏰ 트리거 생존 여부 / ⏱ 실행 로그 |
| `처리중`에서 멈춤 | 10분 뒤 자동으로 `대기` 복귀 후 재시도됨 |
| `등록되지 않은 사용자` | 콘솔 `Access` 탭 이메일 오타 |
| `배포 권한이 없습니다` | `Access` role을 `publisher`로 |
| `GitHub 인증 실패 (401)` | 토큰 만료 → 재발급 후 `Config` 교체 |
| `Drive에 폴더가 없습니다` | 하위 폴더명이 `version` 값과 다름 |
| `OpenAI 404` | `openai_model`을 `gpt-4o-mini`로 변경 |
| **잘못 배포함** | 📋 → `사이트에서 내리기`, 또는 GitHub Commits → Revert |
| 누가 뭘 했는지 | 콘솔 `Log` 탭 (실패·차단 시도 포함) |

---

## 9. 정기 점검

| 주기 | 항목 |
|---|---|
| 차수마다 | 이미지 총량 3MB 이하 유지 |
| 분기 | `Access` 탭 — 퇴사·이동자 정리 |
| 연 1회 | **GitHub 토큰 갱신** (만료 시 배포가 조용히 멈춤) |
| 수시 | OpenAI 사용량·잔액 |

---

## 10. 팀 규칙으로 정해둘 것

1. **번역 발주 후 한국어 원문을 고치지 않는다.** 급하면 새 `key`를 추가한다.
2. **MT 초벌은 감수 없이 배포하지 않는다.** 특히 영·일·중.
3. **`필수 업데이트` 배지는 실제 강제 업데이트 차수에만 쓴다.**
4. **`key`는 한번 정하면 바꾸지 않는다.** 번역 보존의 기준이다.
