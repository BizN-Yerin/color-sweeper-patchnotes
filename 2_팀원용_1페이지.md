# 컬러스위퍼 패치노트 — 팀원 안내서

> 이 문서만 보면 됩니다. GitHub·코드·서버 지식 전혀 필요 없습니다.
> 쓰는 건 **구글 시트 1개 + Drive 폴더 1개**가 전부입니다.
> 본문을 어떤 모양으로 쓸지는 `4_본문_작성규칙.md` 를 보세요.

---

## 시작 전에 한 번만

마스터 시트 상단 **📋 패치노트 → 내 권한 확인**

| 결과 | 할 수 있는 일 |
|---|---|
| 관리자 / 배포자 | 작성 → 번역 → 검증 → **사이트 배포까지** |
| 작성자 | 작성 → 번역 → 검증까지. 배포는 배포자에게 요청 |
| "아직 권한 목록에 없습니다" | 1분 뒤 다시 시도. 계속 그러면 관리자에게 요청 |

---

## 차수당 작업 (20~30분)

### ① 한국어 원문 — `Versions` + `Content` 탭

**Versions 탭에 한 줄**

| 칸 | 뭘 넣나 |
|---|---|
| version | `1.25.0` — 앱 버전 그대로 |
| date | 업데이트 날짜. 제목 위에 표시됩니다 |
| tags | `event` `content` `balance` `fix` `system` 중 콤마 구분 |
| update_type | `필수` 또는 `선택`. 비우면 배지 없음 |
| hero_image_key | 대표 이미지 이름 (보통 `hero`) |
| title_ko | 사이트 제목. 60자 이내 |
| summary_ko | 한두 문장 요약. 200자 이내 |

**Content 탭에 본문을 한 줄씩** — 한 행이 화면의 한 덩어리입니다.

| type | 화면 |
|---|---|
| `text` | 문단 |
| `list` | ● 목록 (연속 행이 하나로 묶임) |
| `table` | 표 (`칸1\|칸2`, 첫 행이 헤더) |
| `image` | 이미지 |
| `period` | 이벤트 기간 카드 |
| `notice` | 안내박스 |

> **기호를 직접 넣지 마세요.** `-` 나 `·` 를 쓰면 두 번 나옵니다. `type`만 정하면 자동입니다.
> **`key`는 한번 정하면 바꾸지 마세요.** 그 문장의 기존 번역이 전부 날아갑니다.
> **`order`는 10, 20, 30**으로. 나중에 중간에 끼워넣기 편합니다.

기획서에서 옮기기 번거로우면 Claude에 기획서를 붙이고 이 프롬프트를 쓰세요:

```
너는 컬러스위퍼(캐주얼 퍼즐, 글로벌 서비스)의 라이브 운영 PM이다.
아래 업데이트 기획서를 유저용 패치노트 원문(한국어)으로 변환해라.

[규칙]
- 내부 용어·스펙 표기(테이블명, 파라미터, 티켓번호)는 전부 제거한다.
- 유저가 얻는 것 중심으로. "무엇이 바뀌었나"가 아니라 "무엇을 할 수 있나".
- 문장은 짧게. 한 블록 2~3문장 이내.
- 수치는 반드시 유지한다 (스테이지 번호, 보상 수량, 확률, 기간).
- 12개 언어로 번역될 것을 전제로, 관용구·말장난·한국 한정 밈은 쓰지 않는다.
- 아래 형식의 TSV로만 출력한다. 헤더 포함.

version	order	section_key	section_title_ko	tag	type	key	ko	image_key	max_len	note

[tag] event / content / balance / fix / system
[type] text / list / table / image / period / notice
[max_len] 제목 60 / 요약 200 / 본문 250 / 캡션 60 / 목록 120 / 표·기간 0

[기획서]
<여기에 붙여넣기>
```

**나온 초안은 반드시 직접 읽고 고치세요.** 톤 결정은 사람이 합니다.

### ② 이미지 — Drive 폴더에 드래그

Config 탭의 `drive_folder_url` 링크 → **차수 이름 그대로** 하위 폴더를 만들고 드래그.

```
1.25.0/
├── hero.png          ← 전 언어 공용
└── hero@ja.png       ← 일본어에서만 교체
```

파일명 앞부분 = `image_key` 칸에 적는 이름 / png·jpg·gif·webp / **개당 2MB 이하**
실제 운영에서는 **300~500KB 권장** — 유저 상당수가 모바일 데이터로 엽니다.

### ③ 번역 시트 만들기

📋 → **1. 번역 시트 생성 / 갱신**

`T_1.25.0` 탭이 생깁니다. 원문을 고친 뒤 다시 생성해도 **기존 번역은 `key` 기준으로 보존**됩니다.

### ④ MT 초벌 (선택)

📋 → **2. MT 초벌 채우기** → 언어 선택 (쉼표 구분 또는 `all`)

- **비어 있는 칸만** 채웁니다. 사람이 번역한 칸은 안 건드립니다
- 채워진 칸은 **노란 배경**으로 표시됩니다
- 번역가에게 "노란 칸만 봐주세요"라고 하면 됩니다

감수가 끝나면 📋 → **감수 완료 처리** → 노란 배경이 지워집니다.

### ⑤ 검증

📋 → **3. 번역 상태 검증**

```
ko : 원문 24행
✅ en       24/24  (100%)
🟡 zh-Hans  18/24  (75%)
⬜ th        0/24  (0%)

⚠️ MT 초벌 미감수 12칸 (노란 배경)
```

### ⑥ 배포

📋 → **5. 사이트에 배포**

- **100% 끝난 언어만** 정식 공개됩니다
- 나머지 언어 유저는 **영어 본문 + 그 언어 안내문**을 봅니다
- 그러니 **12개 언어를 다 기다리지 마세요.** 업데이트 당일 공지가 훨씬 중요합니다
- 나중에 번역이 채워지면 배포만 다시 누르면 됩니다
- 1~2분 뒤 사이트 반영

잘못 올렸으면 📋 → **사이트에서 내리기**. 본문·이미지는 남아 있어서 다시 배포하면 복구됩니다.

---

## 덤: 스토어 업데이트 노트

📋 → **스토어 업데이트 노트 뽑기**

`StoreNotes` 탭에 12개 언어 스토어 문구가 정리되고, 구글 플레이 500자 초과 여부까지 체크됩니다.
**번역 한 번으로 사이트와 스토어를 같이 커버**하는 게 이 구조의 핵심 이득입니다.

---

## 하지 말아야 할 것

1. **번역 발주 후 한국어 원문 고치기** — 번역가 작업 행이 밀립니다. 급하면 새 `key` 추가.
2. **`key` 값 바꾸기** — 그 문장의 기존 번역이 전부 날아갑니다.
3. **MT 초벌을 감수 없이 배포** — 특히 영·일·중.
4. **`필수 업데이트` 남발** — 매번 붙으면 유저가 배지를 안 봅니다.
5. **T_ 시트의 숨겨진 첫 컬럼 건드리기** — 번역과 원문을 잇는 기준입니다.

---

## 막혔을 때

| 증상 | 해결 |
|---|---|
| "등록되지 않은 사용자입니다" | 관리자에게 권한 등록 요청 |
| "배포 권한이 없습니다" | 작성자 권한. 배포자에게 요청 |
| "Drive에 폴더가 없습니다" | 하위 폴더명이 version 값과 같은지 확인 |
| 이미지가 안 보임 | 📋 → 4. 이미지 확인 → `image_key`와 파일명 대조 |
| 표가 `|`로 그대로 보임 | `type`이 `table`인지 확인 |
| 배포 요청이 계속 대기 | 2분 뒤에도 그러면 관리자에게 알림 |
| 배포했는데 사이트 그대로 | 1~2분 대기 → `Ctrl+Shift+R` |
